# Git Config

```
# cursor ~/.gitconfig

[core]
	# git config --global core.ignorecase false
	ignorecase = false
[rebase]
	# git config --global rebase.autoStash true
	autoStash = true
[user]
	email = {{ YOUR_EMAIL }}
	name = {{ YOUR_NAME }}

```
